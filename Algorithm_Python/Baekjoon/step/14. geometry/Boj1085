
#기하 
#기하학의 기본요소는 점,선,다각형,다면체가 있다. 이것을 활용하여 어떻게 표현할것인가 결정해야함
#두번째 직사각형 x,y에서 w,h크기만큼의 직사각형의 차 구하면됨



#백준 1085 직사각형에서 탈출
#직사각형과 점의 거리를 구하기


x, y, w, h = map(int,input().split())
print(min(x, y, w-x, h-y))
